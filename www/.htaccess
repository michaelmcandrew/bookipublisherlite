#if it doesn't start with current or archive, rewrite it to current
Options +FollowSymlinks
RewriteEngine On
RewriteCond %{REQUEST_URI} user/(.*)
RewriteRule !^(user/current|user/archive|user/source) user/current/%1